# xuan/ — LEGACY (Archived)

This was the original LootVue frontend codebase. All code has been migrated to `/lootvue/`.

**Do not add new code here.** The active frontend is at `/lootvue/`.

Migrated on 2026-03-22:
- 18 unique engine files moved to `lootvue/src/lib/engines/`
- All stores, hooks, types, components were already duplicated in lootvue
- All 19 dashboard pages were compacted into 7 pages in lootvue
- Config files (tailwind, next.config, tsconfig) were superseded by lootvue's versions
