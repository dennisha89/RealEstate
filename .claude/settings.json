{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Write",
      "Bash(npm*)",
      "Bash(npx*)",
      "Bash(node*)",
      "Bash(cd*)",
      "Bash(git*)",
      "Bash(mkdir*)",
      "Bash(ls*)",
      "Bash(cat*)",
      "Bash(echo*)",
      "Bash(rm*)",
      "Bash(cp*)",
      "Bash(mv*)",
      "Bash(touch*)",
      "Bash(gh*)",
      "Glob",
      "Grep",
      "Agent",
      "WebSearch",
      "WebFetch"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hook": "cat .claude/scratchpad.md 2>/dev/null"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hook": "bash -c \"FILE='$CLAUDE_FILE_PATH'; if echo $FILE | grep -qE '(calculator|financial-engine|dcf-engine|monte-carlo-engine|waterfall-engine|stress-test-engine|comps-engine|appreciation-engine|cost-insurance-engine|loan-calculator|closing-cost-engine)'; then echo '⚠️ FINANCIAL ENGINE MODIFIED — run /verify-calculation and /test unit before committing'; fi; if echo $FILE | grep -qE 'generateMock'; then echo '⚠️ MOCK DATA DETECTED — ensure this is dev-only code, never ship to production'; fi; if echo $FILE | grep -qE '\\.(env|key|secret|token)'; then echo '🚫 POSSIBLE SECRET FILE — do NOT commit credentials'; fi\""
      }
    ],
    "PreCommit": [
      {
        "hook": "bash -c 'echo \"=== LootVue ===\"; cd lootvue && npx tsc --noEmit 2>&1 | head -15; echo \"=== Property Analyzer ===\"; cd ../property-analyzer && npx tsc --noEmit 2>&1 | head -15'"
      }
    ]
  }
}
